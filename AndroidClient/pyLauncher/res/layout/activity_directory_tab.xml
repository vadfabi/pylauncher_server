<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".DirectoryTab" >

    <ListView
        android:id="@+id/listViewDirectories"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/buttonAdd"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/textViewTag"
        android:layout_marginTop="5dp" >

    </ListView>

    <TextView
        android:id="@+id/textViewTag"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/listViewDirectories"
        android:layout_alignParentTop="true"
        android:text="Directories registered in 'directoryList.txt':" />

    <Button
        android:id="@+id/buttonRemove"
        android:layout_width="120dp"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/buttonAdd"
        android:layout_alignParentBottom="true"
        android:text="Remove" />

    <Button
        android:id="@+id/buttonAdd"
        android:layout_width="120dp"
        android:layout_height="wrap_content"
        android:layout_above="@+id/buttonRemove"
        android:layout_centerHorizontal="true"
        android:text="Add" />

</RelativeLayout>